#Creating certs for each instance that was listed in the instances.yml file.
#https://www.elastic.co/guide/en/elasticsearch/reference/current/certutil.html

instances:
  - name: elasticsearch
    dns:
      - elasticsearch
      - localhost
    ip:
      - 127.0.0.1
      - 172.19.0.3  # Add this line


  - name: kibana
    dns:
      - kibana
      - localhost
    ip:
      - 127.0.0.1
