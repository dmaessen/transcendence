# SECRET_KEY="CHANGE-ME"

# # 0 False, 1 True
# DEBUG="1"

# # Comma Separated values
# ALLOWED_HOSTS="127.0.0.1, localhost"

# Set local volume 
DB_DATA_DIR="$HOME/.tranceanddance"
STATIC_PATH="/static"
MEDIA_PATH="/media"

# Variables for djago and postgres
DB_ENGINE="django.db.backends.postgresql"
POSTGRES_DB="trance"
POSTGRES_USER="tranceanddance"
POSTGRES_PASSWORD="trumpsadness"
POSTGRES_HOST="psql"
POSTGRES_PORT="5432"

#Google credentials

#Elastic credentials
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=banana
KIBANA_PASSWORD=changeme
LOGSTASH_PASSWORD=changeme
ELASTIC_CA_PASSWORD=elastic-ca-pass
ELASTIC_CERT_PASSWORD=elastic-cert-pass
KIBANA_KEYSTORE_PASSWORD=kibana-cert-pass
LOGSTASH_KEYSTORE_PASSWORD=logstash-cert-pass
FILEBEAT_KEYSTORE_PASSWORD=filebeat-cert-pass
ELASTICSEARCH_HOST_PORT=https://elasticsearch:9200